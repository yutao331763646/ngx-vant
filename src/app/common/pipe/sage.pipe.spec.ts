/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SagePipe } from './sage.pipe';

describe('Pipe: Sagee', () => {
  it('create an instance', () => {
    let pipe = new SagePipe();
    expect(pipe).toBeTruthy();
  });
});
